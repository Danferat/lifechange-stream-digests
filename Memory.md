# Memory

## 2026-05-18

- Развернут локальный hardened-пайплайн для выжимок стримов LifeChange.
- Скрипты перенесены из root-oriented гайда в проектную папку `scripts/`.
- Добавлена безопасная работа с Groq ключами через переменные окружения или файлы `.secrets/*.key`.
- Добавлена поддержка локального `.env` с `GROQ_API_KEY` и `GROQ_API_KEY_FALLBACK`.
- Реализованы транскрипция с resume-логикой, smoke test, merge частей, генерация `summary_source.md` и валидатор артефактов.
- Исключены лишние Python-зависимости: HTTP multipart к Groq выполняется стандартной библиотекой Python.
- В `meta.json` сохраняется имя исходного файла без абсолютного локального пути.
- Добавлены README, `.gitignore`, структура `input/`, `work/`, `.secrets/` и локальный unittest для merge.
- Добавлен генератор `scripts/build_context_links.py`, который по выжимке и/или `summary_source.md` создаёт короткий текстовый блок `Полезные ссылки из чата` с упомянутыми темами, сервисами и инструментами без URL.
- Добавлен финальный шаг `scripts/build_final_digest.py`: после AI-выжимки он строит блок тем по полной транскрибации и собирает две версии для выгрузки: `summary_outputs/<stem>_summary_with_links.md` и `summary_outputs/<stem>_summary_short_with_links.md`.
- Валидатор `--check-final` теперь требует обе версии с блоком тем и проверяет, что они начинаются с соответствующих исходников `summary.md` и `summary_short.md`.
- Для последней записи `14_maya` созданы `work/14_maya/summary_outputs/14_maya_summary_with_links.md` и `work/14_maya/summary_outputs/14_maya_summary_short_with_links.md`; устаревшие отдельные `context_links.md` и `final.md` удалены из выдачи.
- Создан `CHANGELOG.md` как сжатое System State проекта.
- Добавлен внешний локальный навык `<codex-home>/skills/cash-cleaner` (`Cash_cleaner`) для безопасной dry-run очистки старых артефактов `work` старше 7 дней или адресной очистки по имени.

## 2026-06-04

- Блок `Полезные ссылки из чата` теперь рендерится списком через `•`, а не markdown-буллетами `-`; README и тесты обновлены под новый формат.
- Пайплайн выжимок переведен на дефолт `summary_short.md` + `summary_intermediate.md`; длинная `summary.md` теперь опциональна и готовится только по запросу.
- Добавлен `scripts/build_intermediate_digest.py` для сборки технической промежуточной выжимки: 48 коротких описаний совмещаются с 48 сырыми блоками `summary_source.md`.
- `scripts/build_final_digest.py` больше не требует длинную `summary.md`: короткая версия с блоком тем собирается всегда, длинная `summary_with_links.md` создается только при наличии `summary.md`.
- Из генератора полезных тем удален слабый кандидат `Скрипко и заморозка депозитов`, который срабатывал на любое упоминание слова `депозит`.

## 2026-06-11

- Формат коротких выжимок обновлен: строки `summary_short.md` теперь не должны заканчиваться точкой; README, глобальные редакторские правила, валидатор и тесты синхронизированы с этим требованием.

## 2026-06-20

- Убран фиксированный `TARGET_LINES = 48`: блоки `summary_source.md` теперь рассчитываются динамически из длительности стрима — `ceil(duration / block_seconds)`, дефолт 240 с (4 мин). Параметр `--block-seconds` в `merge_groq_parts.py`. Количество блоков сохраняется в `meta.json` как `summary_source_lines` и используется валидатором и `build_intermediate_digest.py`.
- Добавлен агентский скилл `.claude/skills/summarize.md`: при любой просьбе сделать выжимку в этом проекте Claude автоматически читает `summary_source.md`, генерирует `summary_intermediate_notes.md` и `summary_short.md` по правилам формата, запускает build-скрипты и валидацию. API-вызов не нужен — агентскую часть берёт на себя Claude Code.
- Создан `CLAUDE.md` в корне проекта с автотриггером: любая формулировка «сделай выжимку / summary / сводку» → немедленный запуск скилла без явного `/summarize`.

## 2026-07-10

- Добавлен отдельный AMA-режим: `scripts/build_ama_timecodes.py` строит кандидаты по сырым `segments.json`, а после проверки формирует `summary_outputs/<stem>_ama_timecodes.md` и `groq_outputs/<stem>_ama_source.md` с границами по вопросам, а не по фиксированным временным блокам.
- Для АМА теперь обязательны два шага: список полных вопросов со скринов и review-файл с подтверждёнными таймкодами до начала чтения вопроса.
- `validate_outputs.py --check-ama` проверяет количество вопросов, порядок таймкодов, подтверждённые границы и вопросный исходник.
- В `CLAUDE.md` и `AGENTS.md` добавлен явный выбор между обычной выжимкой и AMA-таймкодами; упоминание АМА без выбора режима должно вызвать уточняющий вопрос.

## 2026-07-11

- Обработан `stream_10_july` (Robinhood Chain / мем-сезон): транскрибация, `summary_source.md` (28 блоков), три выжимки и валидация.
- Найдена и исправлена проблема: блок `Полезные ссылки из чата` строился только по фиксированному словарю тем в `build_context_links.py` (LINK_CANDIDATES), поэтому в разных стримах повторялись одни и те же "вечнозелёные" темы, а реальные уникальные инструменты стрима (Alchemy, DexScreener, GMGN и т.д.) туда не попадали.
- Добавлен новый обязательный шаг 4в в `.claude/skills/summarize.md`: агент выписывает реальные упомянутые в source инструменты/сервисы в `summary_outputs/<stem>_summary_mentioned_materials.md`.
- `scripts/build_final_digest.py` теперь берёт блок ссылок из этого файла, если он есть; старый поиск по `build_context_links.py` остался только как фолбэк для стримов без этого файла. Добавлен тест `test_mentioned_materials_file_overrides_transcript_link_detection`.
- Отдельно найдена рассинхронизация таймкодов между `summary.md` и `summary_short.md` для одного стрима (одна и та же тема описана с разным таймкодом в короткой и длинной версии). Первая попытка исправить это - скопировать таймкоды блоков из `summary.md` в `summary_short.md` - была признана пользователем неверной: цель не механическое совпадение файлов, а точность каждого таймкода относительно реального момента в аудиозаписи.
- `summary.md` технически ограничена таймкодом старта 4-минутного блока (ровно N строк на N блоков, проверяется `validate_outputs.py`) - точнее там быть не может без смены формата.
- `summary_short.md` не связана построчно с блоками, поэтому для неё таймкоды пересчитаны по мелкой посегментной разметке `groq_outputs/<stem>_transcript.txt` (сегменты ~30-40 секунд) - для каждой строки взят таймкод сегмента, где тезис реально впервые звучит, а не таймкод содержащего его 4-минутного блока.

## 2026-07-17

- Обработан `stream_17_july`: транскрибация (2ч05мин, 1352 сегмента, 32 блока), три выжимки, валидация.
- Найдена системная ошибка в `summary.md`/`summary_intermediate_notes.md`: на длинном source (33 блока, не помещается в один `Read`) факты для нескольких блоков были привязаны к таймкоду следующего блока, а не текущего - топик визуально перетекает через границу 4-минутного блока, и при чтении source большими кусками легко приписать факт из конца блока N таймкоду блока N+1.
- Причина - правило "факт должен звучать ближе к началу блока" в скилле уже было (Шаг 4б), но не было механической проверки, только общее впечатление от прочитанного текста.
- Исправлено: в `.claude/skills/summarize.md` добавлен обязательный Шаг 2б - перед генерацией `summary.md`/`_notes.md` извлекать начало каждого блока отдельной командой (regex-сплит по таймкодам, первые ~400 символов на блок) и сверять каждую строку с этим фрагментом механически, а не по памяти о прочитанном полотне текста.
- Проверено: все 32 блока `stream_17_july` перепроверены по извлечённым фрагментам, 8 строк с неверной привязкой исправлены, build-скрипты пересобраны, `VALIDATION_OK`.

## 2026-07-25

- Обработан `stream_24_july` («Дети волатильности»): транскрибация 1ч59м, 1075 сегментов, 30 блоков, три выжимки и список упомянутых инструментов, `VALIDATION_OK`.
- Пробовали разбивку `summary_short.md` на смысловые разделы с заголовками вида `НАЗВАНИЕ РАЗДЕЛА <таймкод>`. Пользователь забраковал по внешнему виду и попросил откатить - выжимка осталась сплошным списком строк без заголовков и пустых строк. Валидатор, скилл, README и тесты возвращены к прежнему поведению (лимит 19-24 считается по всем строкам файла).
- Пользователь забраковал первую версию `summary_short.md` для `stream_24_july`: строки были надёрганными яркими фактами, а таймкоды указывали в середину блоков, поэтому карта стрима не собиралась (например, блок про AI начинается в 17:55 с фразы "давайте перейдём к AI", а в выжимке стоял 19:04 - момент конкретного тезиса про Kimi внутри блока).
- Исправлено содержательно: `summary_short.md` пересобран как карта смысловых блоков - сначала по транскрипту найдены точки смены темы, затем для каждой взят таймкод сегмента, где тема реально начинается. 24 блока в 6 разделах.
- Исправлено системно: в Шаг 4 `.claude/skills/summarize.md` добавлен обязательный порядок работы - сначала выписать точки перехода темы, найти их таймкоды в `transcript.txt`/`segments.json`, и только потом писать строки; блоков больше 24 - объединять соседние, меньше 19 - дробить длинные. Явно описана типичная ошибка "надёргать 20 ярких фактов".
- Проверено: `python3 -m unittest discover -s tests` - 7 тестов OK, включая новый `test_short_summary_sections_are_validated`; `stream_24_july` пересобран и проходит `--check-final`.

## 2026-09-05

- Обработан стрим по YouTube-ссылке (не локальный файл): `https://www.youtube.com/live/8qcKkcdm458`, «СЛИВАЕМ ИНФУ ИЗ ЛАЙФЧЕЙНДЖ | стрим #2» от 4 сентября, стем `vse_slivaem_info_2`. Аудио скачано через `yt-dlp -x --audio-format mp3` (пришлось обновить yt-dlp через `brew upgrade` - старая версия 2026.3.17 отдавала 403/только сторибоард-картинки на этом видео).
- Транскрибация 2ч20м, 1029 сегментов, 36 блоков, три выжимки (short/intermediate/long) и список упомянутых материалов, `VALIDATION_OK`.
